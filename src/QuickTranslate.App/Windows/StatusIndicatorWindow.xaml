<Window x:Class="QuickTranslate.App.Windows.StatusIndicatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual"
        IsHitTestVisible="False"
        Focusable="False"
        Title="QuickTranslate Status"
        Left="0"
        Top="0">
    <!-- 识别/翻译中的加载指示 pill：深色圆角气泡 + 三点跳动动画 + 文案。
         窗口整体点击穿透（WS_EX_TRANSPARENT，见 code-behind），不打断用户操作。 -->
    <Border x:Name="RootBorder"
            Background="#E60F172A"
            CornerRadius="16"
            Padding="16 9"
            SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12"
                              ShadowDepth="0"
                              Opacity="0.25"
                              Color="Black"/>
        </Border.Effect>
        <StackPanel Orientation="Horizontal">
            <Canvas Width="22" Height="8" VerticalAlignment="Center" Margin="0 0 8 0">
                <Ellipse x:Name="Dot1" Canvas.Left="0" Canvas.Top="2" Width="5" Height="5" Fill="#93C5FD"/>
                <Ellipse x:Name="Dot2" Canvas.Left="8" Canvas.Top="2" Width="5" Height="5" Fill="#60A5FA"/>
                <Ellipse x:Name="Dot3" Canvas.Left="16" Canvas.Top="2" Width="5" Height="5" Fill="#3B82F6"/>
            </Canvas>
            <TextBlock x:Name="MessageText"
                       Text="正在识别…"
                       Foreground="#F1F5F9"
                       FontSize="13"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>
